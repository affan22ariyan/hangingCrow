version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "4001:4001"
    environment:
      - PORT=4001
      - JWT_SECRET=${JWT_SECRET}
      - DB_FILE=${DB_FILE:-data.sqlite}
    # No volume mount in production image to avoid host node_modules conflicts
